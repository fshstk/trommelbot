{
  "build": {
    "dockerfile": "Dockerfile",
    "target": "develop"
  },
  "postCreateCommand": "npm install",
  "runArgs": [
    "--env-file",
    ".env"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker"
      ]
    }
  }
}
